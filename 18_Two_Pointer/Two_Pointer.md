# Two Pointer Technique

**Two indices move through array.**

The **Two Pointer technique** is an algorithmic pattern that uses two indices (pointers) to traverse a data structure, typically an array, string, or linked list, to solve problems efficiently in a single pass.

## Example:

```
left → ← right
```

## Used for:
- Pair sum
- Remove duplicates
